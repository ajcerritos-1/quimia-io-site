import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

// Global root metadata for the whole app (including sign-in). Spanish,
// matching UX-DR22/NFR-9's professional-tone voice — this replaced the
// leftover create-next-app boilerplate (code review P5).
export const metadata: Metadata = {
  title: "Quimia IO",
  description: "Sistema de gestión para laboratorios clínicos.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="es"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
