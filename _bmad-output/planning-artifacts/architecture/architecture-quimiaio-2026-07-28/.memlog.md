---
scope: Quimia IO Phase 1 — single-tenant LIS for the first client (ToronjaLab replacement)
purpose: Architecture spine to keep independently-built epics/stories consistent for Phase 1 implementation
altitude: feature (keeps epics coherent)
updated: 2026-07-30T00:43
---

- (event) Run started. Driving input: prd.md (prd-quimiaio-2026-07-05), status final, updated 2026-07-28 — already reflects sprint-change-proposal-2026-07-27 diffs (FR-74a/FR-74b split, FR-22 container-label extension, FR-35 footnote).
- (constraint) UX EXPERIENCE.md (ux-quimiaio-2026-07-10) capture-screen revision (auto-populated instrument results + container-label print step) is a follow-up flagged in sprint-change-proposal-2026-07-27, NOT YET applied as of this run's start — spine must not assume the UX artifact already models instrument-fed results.
- (assumption) No project-context.md found at project root — no org-wide persistent facts to inherit; treating PRD + addendum.md + sprint-change-proposal-2026-07-27 as the full set of binding inputs.
- (constraint) Owner pre-made stack preferences (addendum.md): Next.js 14+ App Router, TS strict, Tailwind+shadcn/ui, PostgreSQL+RLS (Supabase or Neon), Prisma, NextAuth v5, React-PDF/jsPDF, Twilio WhatsApp, Resend email, qrcode.react, Vercel (P1)->VPS/Railway (P2), Stripe. Explicitly stated as preferences to RATIFY OR CHALLENGE, not fixed requirements.
- (question) Owner flagged tension to resolve in architecture: Vercel serverless (stated P1 host) vs. long-running needs for live HL7/ASTM equipment interfacing (FR-74a) which requires a persistent TCP/serial listener process, not stateless request/response. Also: Supabase vs. Neon for RLS ergonomics.
- (assumption) quimiaio-prompt-maestro.md (project root) holds a draft Prisma schema (Tenant/User/Patient/Order/OrderItem/Study/Analyte/AnalyteRef/Result/Payment + enums, sec 9) and route layout (sec 10) -- 'strong starting sketch, not a contract' per addendum.md. Known gaps: no models yet for Caja, quotations, companies, doctors, inventory, audit log, CFDI docs.
- (constraint) Owner mandates per-module dev methodology: SPEC -> SCHEMA -> API -> UI -> CODE -> REVIEW, Zod validation client+server, RLS on every table, mobile-first components, explicit loading/error states. First module to build: Dashboard + Auth + base layout.
- (direction) User chose Coaching path over Fast path for this architecture run.
- (direction) Deliverable scope confirmed: spine only (build-substrate), audience = owner (AJ) + AI-assisted dev. No additional human-facing artifact (deck/report) requested.
- (decision) AD candidate: FR-74a interfacing uses a LOCAL on-site agent (lightweight Node/TS process) per instrument, installed on the SAME PC/laptop already dedicated to that instrument (client's existing topology: 1 PC per analyzer, running the manufacturer's own software). No new hardware. Agent listens to the instrument's native host interface (HL7/ASTM over local TCP or serial) and pushes results to the cloud app via an authenticated HTTPS API call. Vercel-hosted web app is untouched -- it only receives inbound webhook-style calls, never holds an open device connection. Binds: FR-74a. Prevents: assuming Vercel/cloud can hold a persistent device connection, and assuming new client-site hardware is needed.
- (decision) AD candidate: Design paradigm = Screaming/vertical-slice modular monolith. Domain modules (patients, orders, results-capture, caja, equipment-interfacing, doctors, delivery, audit, etc.) are top-level vertical slices, each owning its routes/server-actions/domain-logic. Cross-module access goes through an explicit interface/service call, never direct reads/writes into another module's Prisma models. Chosen over full hexagonal (too much ceremony for 17-week fixed-budget solo/AI-assisted build) and simple layered (no boundary discipline -- exactly the divergence risk this spine exists to prevent). [ADOPTED by user]
- (version) Verified via web (2026-07-28): Supabase's RLS ergonomics (auth.uid() in policies) are coupled to Supabase Auth specifically; owner's stack already picks NextAuth v5, which neutralizes that advantage -- both platforms require manual session-variable injection for RLS. Neon offers copy-on-write branching (instant, storage-efficient), a fit for the owner's per-module SPEC->SCHEMA dev flow. Sources: designrevision.com/blog/supabase-vs-neon, dev.to Neon-vs-Supabase-2026, prisma.io blog Prisma-Postgres-vs-Neon-pricing-2026.
- (decision) AD candidate: PostgreSQL host = Neon (not Supabase). Binds: all. Prevents: assuming Supabase's built-in RLS/auth.uid() applies -- it does not, since auth is NextAuth v5, not Supabase Auth. [ADOPTED by user]
- (decision) AD candidate: DB access convention = single Prisma client wrapper (extended client / repository entrypoint) is the ONLY path to the database. Wrapper opens a transaction, runs SET LOCAL app.tenant_id / app.role from the authenticated session (NextAuth) before any query; Postgres RLS policies read via current_setting(). Direct prisma.<model> calls outside the wrapper are forbidden. Binds: all modules. Prevents: a module (human or AI-agent-built) silently bypassing tenant isolation via a raw Prisma call. [ADOPTED by user]
- (decision) AD candidate: Local-agent-to-cloud auth = per-instrument API key + HMAC request signing (body+timestamp), not bare bearer key or mTLS. Binds: FR-74a ingestion endpoint. Prevents: replay attacks and key-in-plaintext exposure, without requiring certificate management on an unmanaged lab PC. [ADOPTED by user]
- (decision) CORRECTION (supersedes prior Sample/Container AD candidate): user showed real label photos. The barcode/code is IDENTICAL across all tubes of one order (tube color is only a visual draw-guide for phlebotomy, not read by instruments). No separate Sample/Container entity is needed for equipment-interfacing matching. AD candidate revised: Order carries its existing 'code' field (date+daily-reset-sequence folio, e.g. 2707260001), unique per (tenant_id, code) not globally. Incoming instrument result = {code, analyte_code, value}; API resolves Order by (tenant_id from instrument's API key, code), validates the analyte belongs to a Study the Order actually requested, writes Result. Binds: FR-74a, FR-22. Prevents: inventing a per-tube identity model the client's real workflow doesn't have.
- (assumption) Structural seed note: Study/Panel catalog needs a tubeType/tubeColor attribute (e.g. amarillo/lila/rojo) to drive how many container labels print per order and which color -- catalog data, not a new entity. Needed for FR-22 label printing regardless of equipment interfacing.
- (constraint) Client confirmed (2026-07-28) additional lab equipment exists beyond the 2 named Mindray instruments -- e.g. Finecare Wondfo, a microscope -- but explicitly wants these to STAY MANUAL (low volume, manual entry is acceptable). This validates FR-74a's scope boundary (named 2 instruments only) against real client reality; no scope change. Results-capture screen must support a MIX of auto-populated analytes (from the 2 interfaced instruments) and fully-manual analytes (from other equipment) within the SAME order -- already anticipated by FR-74a's wording.
- (version) Verified via web (2026-07-28): Next.js stable = 16.2.x (owner's stated '14+' is stale). Prisma stable = 7.x, Rust-free TS-native runtime (helps Vercel cold starts vs old Rust engine binary); RLS still requires manual Client Extension wrapper (community pattern 'Yates' matches our AD). Tailwind CSS v4 + shadcn/ui (now defaults to Base UI over Radix) is the current new-project path. Node.js 24 = current Active LTS, recommended for new projects -- pinned for the on-site equipment-interfacing agent runtime.
- (decision) AD candidate: Auth = Better Auth (not NextAuth v5) -- Auth.js v5 is maintenance-mode only per its own maintainers, who redirect new projects to Better Auth. [ADOPTED by user after tradeoff shown]
- (decision) Stack table finalized: Next.js 16.2.x (App Router) - TypeScript 5.x strict - Tailwind CSS v4 + shadcn/ui (Base UI) - PostgreSQL via Neon (RLS) - Prisma 7.x + Client Extension wrapper - Better Auth - React-PDF/jsPDF (documents) - Resend (email, P1) - qrcode.react (portal QR, P1) - Vercel (P1 host for Next.js app) - Node.js 24 LTS (on-site equipment-interfacing agent runtime, per-instrument PC) - Twilio WhatsApp (P2) - Stripe (P2) - VPS/Railway (P2 rehost).
- (decision) AD candidate: Deployment/environments = dev(local) -> preview(Vercel preview deploy + ephemeral Neon branch per PR) -> production(Vercel prod + Neon main branch). On-site equipment-interfacing agent runs ONLY in production on the client's lab PCs; dev/testing uses simulated HL7/ASTM messages, never real hardware. Agent API key lives in a local config file on the lab PC (never in source control); server-side secrets live in Vercel/Neon env vars. [ADOPTED by user]
- (decision) AD candidate: Consistency conventions -- Naming: Prisma models PascalCase singular, fields camelCase, API routes kebab-case, domain events 'module.action' (e.g. orders.result-received). Data/formats: IDs = cuid2, dates = ISO-8601 UTC in DB (localize to Mexico time only at presentation layer), API errors = single envelope {error:{code,message,details?}} across all modules. Cross-cutting: all mutations go through the Prisma wrapper (AD already logged), structured JSON logging carrying tenant_id/request_id on every line, typed env config via Zod (no raw process.env in business logic). [ADOPTED by user]
- (decision) AD candidate: ratify owner's draft Prisma schema (quimiaio-prompt-maestro.md sec 9: Tenant/User/Patient/Order/OrderItem/Study/Analyte/AnalyteRef/Result/Payment) as the base ERD, with 2 fixes: (1) Order.folio gets a composite unique index on (tenantId, folio) -- not globally unique, matches the daily-reset code confirmed by the client's real labels; (2) Result gains a 'source' enum (MANUAL | INSTRUMENT) so the capture screen (FR-35) can visually distinguish auto-populated analytes from manually typed ones. Known gaps (per addendum.md, unresolved, deferred to tasks/schema-authoring time, not this spine): Doctor, CashSession/Caja, AuditLog models needed for P1 FRs but not yet drafted; Company/Quotation/Inventory/CFDI models are P2. [ADOPTED by user]
- (decision) AD candidate: source tree = src/modules/{auth,patients,orders,catalog,results-capture,equipment-interfacing,cash,doctors,delivery,audit,kanban}/ (vertical slices per Screaming paradigm), src/shared/{db,config}/ (Prisma wrapper, typed env), agents/mindray-agent/ (separate Node 24 process, runs on client lab PC, not deployed with the Next.js app). kanban module reads orders data, does not own it. [ADOPTED by user]
- (event) Coaching elicitation complete. Proceeding to Finalize: distill spine from memlog, reconcile inputs, reviewer gate.
- (event) Resuming session to run the Finalize pass that was never completed (memlog previously ended at 'Coaching elicitation complete. Proceeding to Finalize'). Full finalize requested by AJ: re-distill, reconcile-inputs pass, Reviewer Gate, close.
- (decision) Deferred item 'Capture-screen UX revision... not yet applied' resolved: bmad-ux ran an Update pass (2026-07-29) adding the Automático/Calculado source-tag cross-reference and an Order print-preview panel (FR-22: container labels + payment ticket + work-order shown before printing, confirmed by user rather than silent auto-print). EXPERIENCE.md/DESIGN.md both status: final, updated 2026-07-29. This Deferred line is cleared from the spine on re-distill.
- (decision) Reconcile pass (4 subagents vs PRD, addendum, sprint-change-proposal, prompt-maestro sec 9-10) found real gaps in the pre-Finalize spine. AD candidate: Audit-log write path = every module writes AuditLog rows only through the same Prisma wrapper entrypoint already mandated for tenant isolation; the AuditLog table's DB role has no UPDATE/DELETE grant (enforced at Postgres privilege level, not just app discipline). Binds: all modules (FR-52, NFR-7). Prevents: a module bypassing the audit path, or an admin silently editing/deleting audit history. [ADOPTED]
- (decision) AD candidate: Kanban/dashboard real-time updates = client-side polling within the NFR-5 SLA window, not WebSocket/SSE. Binds: results-capture/kanban live views. Prevents: one module building a persistent-connection channel while another polls, splitting the real-time story and adding infra (sticky connections) that doesn't fit Vercel's serverless model or the solo-dev operational constraint. [ADOPTED]
- (decision) AD candidate: Backup/restore (NFR-8) = rely on Neon's built-in continuous backup / point-in-time-restore (already adopted for its branching benefits, AD-2); Phase 1 adds a documented quarterly restore-drill as an operational task, not a custom backup system to build. Binds: db/config. Prevents: a module or agent assuming a bespoke backup job needs to be written. [ADOPTED]
- (version) Verified via web (2026-07-30): next-pwa is archived/unmaintained (archived 2023); its actively-maintained successor is Serwist (@serwist/next), the going choice for a PWA on Next.js 16 app router. Binds NFR-6 (PWA/offline floor). Source: aurorascharff.no, blog.logrocket.com, javascript.plainenglish.io coverage of Serwist as next-pwa's recommended successor.
- (constraint) Reconcile flagged AD-4/AD-6's 'daily-reset folio' claim as unsupported by PRD FR-22 text alone (FR-22 only says 'unique per-tenant folio'). Clarifying: the daily-reset scheme came from real client label photos the user showed during this coaching session (see earlier CORRECTION entry), not from PRD text — spine wording amended to cite that evidence explicitly instead of implying it's PRD-derived. No design change: the (tenantId, folio) composite unique index is still collision-safe since the date prefix embedded in the folio string already distinguishes different days.
- (decision) Consistency conventions AD broadened: Zod applies to typed env config (already logged) AND to all API request/response payload validation, per addendum.md's general validation mandate — not scoped to env vars alone. [ADOPTED]
- (gap) Deferred additions from reconcile: (1) altitude engine (Phase 2 SaaS differentiator, addendum.md) — no Phase 1 architectural footprint; (2) Study's 4 dangling catalog FKs (sampleTypeId, methodId, techniqueId, equipmentId, prompt-maestro.md) — same treatment as the already-deferred containerId/Doctor/CashSession/AuditLog gaps, resolved at module SPEC-to-SCHEMA time; (3) User.roleId/Role (RBAC model, Phase 1 per FR-3/NFR-2/Usuarios y Roles) — added to the existing Doctor/CashSession/AuditLog Deferred bullet, since it's the same 'needed for Phase 1 FRs, not yet drafted' bucket; (4) OrderItem.status (ItemStatus enum, prompt-maestro.md) — enum values never specified anywhere, deferred rather than invented; (5) User.branchId/Order.branchId (multi-branch, Red plan) — folded into the existing Phase-2 Company/Quotation/Inventory/CFDI Deferred bullet.
- (note) prompt-maestro.md sec 10's flat Next.js file structure is fully superseded by AD-1's src/modules/* vertical-slice tree; that divergence was made but never explicitly logged as a correction to a cited source. Adding one line to the spine's source-tree section citing it explicitly, since sec 10 is named in this spine's own sources: frontmatter.
- (event) Re-distilled ARCHITECTURE-SPINE.md from the reconcile-pass findings: added AD-10 (audit immutability), AD-11 (Kanban polling), AD-12 (backup/restore via Neon); amended AD-4 to cite label-photo evidence + collision-safety note; broadened the Zod consistency convention to request/response validation; added Serwist to Stack (NFR-6); logged the §10 source-tree divergence explicitly; extended Deferred with User.roleId/Role, Study's 4 dangling catalog FKs, OrderItem.status enum, branch-scoping fields, and the altitude engine; extended Capability Map with NFR-5/6/7/8 rows; extended frontmatter binds. Removed the now-resolved Capture-screen-UX-revision Deferred bullet. Proceeding to Reviewer Gate.
- (event) Reviewer Gate ran: rubric walker (3 critical/6 high/7 medium/4 low), version/reality-check lens (Next.js CVE, TS staleness, wrong React-PDF package, stale qrcode.react, Prisma 7 setup gaps), adversarial-incompatibility lens (Result ownership, AD-3/AD-8 contradiction, pipeline-state 3-representations, AD-4-vs-Deferred branchId contradiction, AD-10 no-op under Neon owner-role semantics). Full reports in reviews/.
- (decision) Triage — autofixed without needing further user input (each resolvable from an already-adopted rule or already-decided UX/PRD fact, not a fresh design fork): Next.js pinned >=16.2.11 (CVE-2026-64642 fix); TypeScript pinned 6.x (not 7, Next.js support is canary-only); React-PDF row corrected to @react-pdf/renderer (the generator, not the wojtekmaj viewer package); qrcode.react flagged stale/re-verify, not blocking; Prisma 7 setup gotchas (prisma.config.ts, adapter-pg, no env auto-load) noted; AD-3 amended with an explicit bootstrap-mode (login/portal/API-key resolve tenantId before a scoped transaction opens), closing the 3-flow gap the rubric walker and adversarial lens both independently found; AD-2 amended with FORCE ROW LEVEL SECURITY + non-owner app role (closes AD-10's owner-bypass no-op too); AD-6 amended so equipment-interfacing calls results-capture's recordInstrumentResult() interface instead of writing Result directly (closes the Result-ownership gap, per-analyte upsert not batch replace, closing the concurrency race too); AD-4 gained two more corrections: OrderItem.status is a stored 6-value enum matching EXPERIENCE.md's already-decided pipeline states (the 'never specified' Deferred justification was factually wrong — UX settled this 2026-07-29), and Result is mutable with every correction paired atomically with an AuditLog write in the same wrapper call (AD-10's immutability applies to AuditLog, not Result); AD-10 gained AuditLog's minimum required shape (cross-cutting entity, can't stay fully undrafted); AD-11 pinned to SWR polling every 4s via one shared hook; AD-8 gained NFR-2 password-policy binding; Branch/branchId moved from Phase-2-Deferred to Phase-1 (Caja/FR-46 already needs it, avoids a week-5-vs-week-13 migration collision); added SWR to Stack; added Observability and Neon-branch-cap as lightly-defaulted Deferred items (Vercel-native logs for now, plan-tier check later) rather than open questions, since neither blocks Phase 1 at solo-dev scale.
